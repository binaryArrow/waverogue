{
  "name": "waverogue",
  "version": "1.0.0",
  "description": "a roguelike wave survival game",
  "source": "src/index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel",
    "build": "parcel build src/index.html --public-url ./",
    "deploy": "gh-pages -d dist"
  },
  "author": "Can Cihan Nazlier",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.5.0"
  }
}
