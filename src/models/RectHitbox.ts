export class RectHitbox {
    posX: number
    posY: number
    width: number
    height: number

    constructor(posX: number, posY: number, width: number, height: number) {
        this.posX = posX
        this.posY = posY
        this.width = width
        this.height = height
    }
}